# 快速开始指南

## 5分钟上手使用

### 步骤1: 安装依赖

```bash
npm install
```

### 步骤2: 启动应用

```bash
npm run dev
```

这将启动：
- 前端开发服务器: http://localhost:3000
- 后端API服务器: http://localhost:3001

### 步骤3: 准备Git仓库

#### 方案A: 使用当前仓库（默认）
工具会自动使用当前目录作为Git仓库

#### 方案B: 使用其他仓库
1. 点击顶部的仓库路径旁的编辑按钮
2. 输入Git仓库的绝对路径
3. 点击"确定"

### 步骤4: 对比两个仓库的代码

如果你需要对比**公司仓库**和**客户仓库**的代码：

```bash
# 创建一个新的Git仓库用于对比
mkdir code-comparison && cd code-comparison
git init

# 添加两个远程仓库
git remote add company /path/to/company/repo
git remote add customer /path/to/customer/repo

# 拉取两个仓库的代码
git fetch company
git fetch customer

# 创建本地分支指向两个仓库
git checkout -b company-main company/main
git checkout -b customer-main customer/main
```

然后在工具中：
1. 设置仓库路径为 `/path/to/code-comparison`
2. 选择 `company-main` 作为基准分支
3. 选择 `customer-main` 作为目标分支
4. 查看所有差异文件

### 步骤5: 查看代码差异

1. 在分支选择器中选择两个要对比的分支
2. 左侧文件列表会显示所有变更的文件
3. 点击任意文件查看详细差异
4. 使用搜索框快速过滤文件
5. 切换"统一视图"或"并排视图"查看差异

## 主要功能

### 🔍 搜索文件
在文件列表的搜索框中输入关键词，快速找到目标文件

### 📊 查看统计
每个文件显示：
- 新增行数（绿色）
- 删除行数（红色）
- 总变更数

### 👁️ 切换视图
- **统一视图**: 传统Git diff风格，单列显示
- **并排视图**: 左右对比，更清晰

### 🔄 切换仓库
随时可以切换到不同的Git仓库进行对比

### ⚡ 性能优化
- 按需加载文件内容
- 大文件流畅滚动
- 快速搜索和过滤

## 常见问题

### Q: 如何对比两个完全独立的仓库？
A: 将两个仓库作为远程仓库添加到一个新的Git仓库中，然后创建对应的本地分支。详见上面的"步骤4"。

### Q: 可以对比多个分支吗？
A: 一次只能对比两个分支，但可以随时切换分支进行新的对比。

### Q: 支持哪些文件类型？
A: 支持所有文本文件的对比，包括代码文件、配置文件、文档等。

### Q: 如何导出对比结果？
A: 目前可以直接复制浏览器中的差异内容。未来版本会支持导出为报告。

## 提示和技巧

💡 **快捷键**（未来会添加）:
- 待开发：上/下键导航文件
- 待开发：Ctrl/Cmd + F 搜索

💡 **最佳实践**:
- 选择有意义的基准分支（通常是主分支）
- 使用搜索功能快速定位关键文件
- 并排视图更适合查看大量变更
- 统一视图更适合快速浏览

💡 **性能建议**:
- 首次加载可能需要几秒钟
- 大型仓库建议使用文件搜索功能
- 定期刷新以获取最新分支信息

## 下一步

- 阅读完整的 [README.md](README.md) 了解更多功能
- 查看 [API 文档](README.md#api-接口) 了解后端接口
- 自定义配置和主题

祝你使用愉快！🚀
