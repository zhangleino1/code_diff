# Markdown 导出功能使用指南

## 📥 功能概述

导出功能允许您将两个分支之间的代码对比结果导出为详细的Markdown文档，方便离线查看、分享和归档。

---

## 🚀 如何使用

### 步骤1: 选择分支

1. 在分支选择器中选择**基准分支**（通常是主分支或稳定分支）
2. 选择**目标分支**（要对比的分支）
3. 确保两个分支不同

### 步骤2: 导出报告

1. 点击顶部导航栏右侧的**绿色"导出报告"按钮**
2. 工具会自动：
   - 收集分支信息
   - 统计变更数据
   - 生成完整的diff
   - 格式化为Markdown
3. 浏览器会自动下载生成的报告文件

### 步骤3: 查看报告

报告文件名格式：
```
代码对比报告_基准分支_vs_目标分支_日期.md
```

例如：
```
代码对比报告_main_vs_feature-auth_2025-12-30.md
```

---

## 📄 报告内容结构

### 1. 基本信息

| 项目 | 说明 |
|------|------|
| 仓库路径 | Git仓库的绝对路径 |
| 基准分支 | 对比的基准分支名称 |
| 目标分支 | 对比的目标分支名称 |
| 生成时间 | 报告生成的时间戳 |

### 2. 分支信息

每个分支包含：
- 最新提交的哈希值
- 作者信息
- 提交时间
- 提交信息

### 3. 变更统计

| 统计项 | 说明 |
|--------|------|
| 变更文件数 | 总共有变更的文件数量 |
| 新增行数 | 所有文件新增的代码行数（绿色） |
| 删除行数 | 所有文件删除的代码行数（红色） |
| 总变更行数 | 新增和删除的总和 |

### 4. 变更文件列表

文件按类型分组：

#### 新增文件
列出所有新增的文件及其新增行数

#### 删除文件
列出所有删除的文件及其删除行数

#### 修改文件
列出所有修改的文件及其新增/删除行数

### 5. 详细差异

展示所有文件的完整代码差异，使用diff格式：

```diff
- 删除的代码行（红色）
+ 新增的代码行（绿色）
  未变更的代码行
```

---

## 💡 使用场景

### 1. 代码审查

**适用场景**：
- Pull Request 审查
- 功能分支验收
- 代码质量检查

**优势**：
- 离线审查，不依赖网络
- 完整的差异记录
- 可添加审查意见

**示例工作流**：
```bash
# 1. 导出功能分支对比主分支的报告
# 在工具中选择: main vs feature-new-api

# 2. 将报告发送给审查人员
# 审查人员可以离线查看并添加评论

# 3. 在团队会议上讨论报告内容
```

### 2. 项目交付

**适用场景**：
- 客户交付文档
- 版本发布说明
- 项目进度报告

**优势**：
- 专业的文档格式
- 完整的变更记录
- 易于理解和查阅

**示例**：
```markdown
# 项目交付报告
## 本次交付内容

[附加导出的对比报告]

对比 v1.0 和 v2.0 版本的所有变更...
```

### 3. 团队分享

**适用场景**：
- 技术分享会
- 代码walkthrough
- 知识传递

**优势**：
- 清晰的变更展示
- 支持注释和讲解
- 可作为培训材料

### 4. 客户仓库对比

**适用场景**：
- 对比公司标准版和客户定制版
- 分析客户需求
- 生成定制化差异报告

**工作流**：
```bash
# 1. 创建对比仓库
mkdir comparison-repo && cd comparison-repo
git init

# 2. 添加两个远程仓库
git remote add company /path/to/company/repo
git remote add customer /path/to/customer/repo

# 3. 拉取并创建分支
git fetch company && git fetch customer
git checkout -b company-main company/main
git checkout -b customer-main customer/main

# 4. 在工具中导出对比报告
# 选择 company-main vs customer-main
# 点击"导出报告"

# 5. 将报告提交给项目经理或客户
```

---

## 📝 报告示例

### 基本信息部分
```markdown
## 基本信息

| 项目 | 内容 |
|------|------|
| **仓库路径** | `/home/user/my-project` |
| **基准分支** | `main` |
| **目标分支** | `feature-auth` |
| **生成时间** | 2025-12-30 10:30:45 |
```

### 统计部分
```markdown
## 变更统计

| 统计项 | 数量 |
|--------|------|
| **变更文件数** | 15 |
| **新增行数** | +234 |
| **删除行数** | -89 |
| **总变更行数** | 323 |
```

### 文件列表部分
```markdown
### 新增文件 (3)

| 文件路径 | 新增行数 |
|----------|----------|
| `src/auth/login.js` | +85 |
| `src/auth/register.js` | +92 |
| `tests/auth.test.js` | +57 |
```

---

## 🎨 最佳实践

### 1. 命名规范

建议的文件命名格式：
```
对比报告_[项目名]_[基准分支]_vs_[目标分支]_[日期].md
```

例如：
```
对比报告_ERP系统_v1.0_vs_v2.0_2025-12-30.md
对比报告_API服务_主分支_vs_开发分支_2025-12-30.md
```

### 2. 组织报告

**建议目录结构**：
```
project/
├── docs/
│   └── comparison-reports/
│       ├── 2025-12/
│       │   ├── report_2025-12-15.md
│       │   └── report_2025-12-30.md
│       └── archive/
│           └── 2025-11/
```

### 3. 添加注释

在导出的报告中，可以手动添加注释：

```markdown
### 修改文件

| 文件路径 | 新增 | 删除 | 总变更 | 备注 |
|----------|------|------|--------|------|
| `src/api.js` | +45 | -12 | 57 | 优化API性能 ✅ |
| `src/db.js` | +23 | -8 | 31 | 需要测试 ⚠️ |
```

### 4. 版本控制

将重要的对比报告纳入版本控制：

```bash
# 将报告提交到Git
git add docs/comparison-reports/report_*.md
git commit -m "docs: 添加分支对比报告"
```

---

## 🔧 高级技巧

### 1. 自定义报告

虽然导出功能生成标准格式，但您可以手动编辑报告：

- 添加执行摘要
- 补充说明信息
- 添加图表和截图
- 标注重要变更

### 2. 批量导出

如果需要对比多个分支：

1. 依次选择不同的分支组合
2. 导出多个报告
3. 整理到同一文件夹
4. 创建索引文件

**索引示例**：
```markdown
# 分支对比报告索引

## v2.0 发布准备

1. [main vs dev](./main_vs_dev_2025-12-25.md)
2. [main vs feature-a](./main_vs_feature-a_2025-12-26.md)
3. [main vs feature-b](./main_vs_feature-b_2025-12-27.md)
```

### 3. 转换为其他格式

使用工具将Markdown转换为其他格式：

```bash
# 转换为PDF
pandoc report.md -o report.pdf

# 转换为HTML
pandoc report.md -o report.html

# 转换为Word
pandoc report.md -o report.docx
```

---

## ⚙️ 技术细节

### 对比模式

工具使用 **文件级对比模式**：

- 基于 `git diff branch1...branch2`
- 对比两个分支的最终状态
- 不考虑中间提交历史
- 展示文件的净变更

### 数据来源

报告中的数据来自：

1. **分支信息**: `git log -1`
2. **文件统计**: `git diff --stat`
3. **详细差异**: `git diff --unified=5`

### 性能考虑

- 大型仓库（>1000个文件变更）可能需要几秒钟
- 报告大小取决于代码变更量
- 建议文件大小 < 10MB 以保证浏览器兼容性

---

## 🐛 故障排除

### 问题1: 导出按钮是灰色的

**原因**: 未选择两个分支或分支相同

**解决方案**:
1. 确保选择了基准分支
2. 确保选择了目标分支
3. 确保两个分支不同

### 问题2: 下载的文件是空的

**原因**: 两个分支没有差异

**解决方案**:
- 检查是否选择了正确的分支
- 使用 `git log` 确认分支确实有差异

### 问题3: 报告太大无法打开

**原因**: 变更文件太多或文件太大

**解决方案**:
1. 使用文件搜索功能，只查看特定文件
2. 分批对比（例如按目录）
3. 使用专业的Markdown编辑器（如 Typora, VS Code）

### 问题4: 中文显示乱码

**原因**: 编码问题

**解决方案**:
- 使用支持UTF-8的编辑器打开
- 推荐使用：VS Code, Typora, Obsidian

---

## 📚 相关资源

- [README.md](README.md) - 完整功能文档
- [QUICKSTART.md](QUICKSTART.md) - 快速开始指南
- [TESTING.md](TESTING.md) - 测试指南

---

## 💬 反馈与建议

如果您对导出功能有任何建议或问题：

1. 提交 GitHub Issue
2. 查看常见问题
3. 参考测试文档

---

**生成时间**: 2025-12-30
**版本**: 1.0.0
